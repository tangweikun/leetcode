import { combinationSum4 } from '.'

test('Combination Sum IV', () => {
  expect(combinationSum4([1, 2, 3], 4)).toBe(7)
})

test('Combination Sum IV', () => {
  expect(combinationSum4([1, 2], 4)).toBe(5)
})

test('Combination Sum IV', () => {
  expect(combinationSum4([1, 2], 2)).toBe(2)
})

test('Combination Sum IV', () => {
  expect(combinationSum4([1, 2], 2)).toBe(2)
})

test('Combination Sum IV', () => {
  expect(combinationSum4([2, 1], 2)).toBe(2)
})
